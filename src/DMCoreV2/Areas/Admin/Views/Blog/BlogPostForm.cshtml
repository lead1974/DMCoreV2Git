@model DMCoreV2.Areas.Admin.ViewModels.Blog.BlogPostViewModel.BlogPostForm

@using System.Collections.Generic
@using Microsoft.AspNetCore.Http
@using Microsoft.AspNetCore.Http.Authentication
@using DMCoreV2.DataAccess.Models
@inject SignInManager<AuthUser> SignInManager

@{
    ViewData["Title"] = Model.IsNew ? "Create Post" : "Update Post";
}

<div class="col-lg-12">
    <h1 class="page-header">@(Model.IsNew ? "Create Post" : "Update Post")</h1>
    <div class="row">
        <section>
            <form asp-controller="BlogPost" asp-route-returnurl="@ViewBag.ReturnUrl" method="post" class="form-horizontal" asp-antiforgery="true" asp-area="admin">
                <div class="col-lg-8">
                    <div asp-validation-summary="All" class="text-danger"></div>
                    <input asp-for="Id" type="hidden" />
                    <div class="form-group">
                        <label asp-for="Title" class="col-md-2 control-label"></label>
                        <div class="col-md-10">
                            <input asp-for="Title" class="form-control focus" type="text" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label asp-for="Slug" class="col-md-2 control-label"></label>
                        <div class="col-md-10">
                            <input asp-for="Slug" class="form-control" type="text" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label asp-for="Body" class="col-md-2 control-label"></label>
                        <div class="col-md-10">
                            <input asp-for="Body" class="form-control" type="text" />
                        </div>
                    </div>
                </div>
                <div class="col-lg-4">
                    <div class="form-group">
                        <div class="panel panel-info">
                            <div class="panel-heading">Post Actions</div>
                            <div class="panel-body">
                                <button type="submit" value='@(Model.IsNew?"CreatePost":"UpdatePost")' name='action' class="btn btn-primary">@(Model.IsNew ? "Create Post" : "Update Post")</button>
                                <button type="submit" value='Cancel' name='action' class="btn btn-default"> Cancel</button>
                            </div>
                        </div>
                    </div>
                </div>
            </form>
        </section>
    </div>
</div>